import { FC } from "react";
import { ChatInputWrapper, ChatInputForm } from "./styled";

const ChatInput: FC = () => {
  return (
    <ChatInputWrapper>
      <ChatInputForm />
    </ChatInputWrapper>
  );
};

export default ChatInput;
